{
	"schemaVersion": 1,
	"id": "${modid}",
	"version": "${version}",
	"name": "${name}",
	"description": "${name} for ${loader} ${minecraft_version}",
	"authors": [
		"${author}"
	],
	"contact": {
		"homepage": "${homepage}",
		"sources": "${sources}"
	},
	"license": "${license}",
	"entrypoints": {
	},
	"mixins": [
	],
	"depends": {
		"minecraft": ">=1.21",
		"fabricloader": "*",
		"fabric-api": "*"
	}
}