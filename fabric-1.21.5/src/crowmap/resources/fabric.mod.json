{
	"schemaVersion": 1,
	"id": "${modid}",
	"version": "${version}",
	"name": "${name}",
	"description": "Maps update anywhere in your inventory.",
	"authors": [
		"${author}"
	],
	"contact": {
		"homepage": "${homepage}",
		"issues": "${issues}",
		"sources": "${sources}"
	},
	"license": "${license}",
	"icon": "${modid}_icon.png",
	"environment": "*",
	"mixins": [
		"crowmap.mixins.json"
	],
	"depends": {
		"minecraft": ">=${minecraft_version}"
	}
}