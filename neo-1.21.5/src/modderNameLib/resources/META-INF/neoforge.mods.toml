modLoader = "javafml"
loaderVersion = "[1,)"
license = "${license}"
issueTrackerURL = "${issues}"

[[mods]]
modId = "${modid}"
version = "${version}"
displayName = "${name}"
description = "${description}"
authors = "${author}"
