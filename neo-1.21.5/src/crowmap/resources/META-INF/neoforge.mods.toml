modLoader = "javafml"
loaderVersion = "[1,)"
license = "${license}"
issueTrackerURL = "${issues}"
logoFile = "${modid}_icon.png"

[[mods]]
modId = "${modid}"
version = "${version}"
displayName = "${name}"
description = "Maps update anywhere in your inventory."
authors = "${author}"

[[mixins]]
config = "crowmap.mixins.json"