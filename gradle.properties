# yeah
org.gradle.jvmargs=-Xmx4G

# it works?
org.gradle.configuration-cache=true

# set this to ".1" or ".2" if releasing more than one version in the same day
versionDisambig=

# version of fabric-loader to use across all fabric subprojects
floaderVersion=0.16.14